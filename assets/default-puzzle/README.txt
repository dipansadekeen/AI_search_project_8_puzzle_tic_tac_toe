The default numbered puzzle is rendered with CSS-drawn tiles, so no image
binaries are required to run the app. Uploaded images are sliced in the browser
at runtime (see js/module_a/image_slicer.js) and never written to disk.

This folder is kept as the conventional home for static puzzle assets.
